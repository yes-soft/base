angular.module('app')
    .controller('app.dashboard', ['$scope', '$stateParams', 'utils',
        function ($scope, $stateParams, utils) {
            var self = $scope;
            self.init = function () {

            };
            self.init();


        }]);