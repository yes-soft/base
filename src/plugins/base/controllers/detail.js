"use strict";
angular.module('app')
    .controller('app.wrap.detail', ['$scope', '$stateParams', '$timeout', '$location',
        function ($scope, $stateParams, $timeout, $location) {
            var self = $scope;
        }
    ]);