angular.module('app')
    .controller('app.dashboard.index', ['$scope', '$stateParams', 'utils', 'ENV', '$log',
        function ($scope, $stateParams, utils, ENV, $log) {
            var self = $scope;
            self.init = function () {

            };
            self.init();


        }]);